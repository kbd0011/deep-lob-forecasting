torch>=2.3
numpy>=1.26
pandas>=2.2
scikit-learn>=1.5
scipy>=1.13
matplotlib>=3.9
einops>=0.8
hydra-core>=1.3
omegaconf>=2.3
# dev
pytest>=8.2
ruff>=0.5
mypy>=1.10
# optional (lazy-imported, only when wandb logging is enabled): wandb>=0.17
# do NOT use mlfinlab (not free); use the vendored shared/validation.py
